# FastAPI Backend Environment
# Path to SQLite database file (absolute or relative)
SQLITE_DB=./data/social_media.db

# Comma-separated origins allowed for CORS (include frontend dev server)
CORS_ALLOW_ORIGINS=http://localhost:3000

# Session TTL in minutes (demo only)
SESSION_TTL_MINUTES=60
