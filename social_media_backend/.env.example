# Path to SQLite database file used by the backend
SQLITE_DB=/app/data/social_media.db

# Comma-separated list of origins allowed for CORS (use * for all)
CORS_ALLOW_ORIGINS=http://localhost:3000,*

# Session time-to-live in minutes
SESSION_TTL_MINUTES=120
